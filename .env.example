# Docker Compose build-time variable — passed into the frontend image
# For local Docker: http://localhost:3000
# For Codespaces/remote: use your forwarded backend URL
VITE_API_BASE_URL=http://localhost:3000
